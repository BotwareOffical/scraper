{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "pino": "^9.6.0",
    "playwright": "^1.50.0"
  },
  "scripts": {
    "start": "node app.js",
    "build": "npx playwright install-deps && npx playwright install chromium"
  }
}